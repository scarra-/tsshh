import React from 'react'
import { TopBar } from './../TopBar'
import { Footer } from './../Footer'

type Props = {
  children: React.ReactNode
}

export const MainSearchLayout = ({ children }: Props) => <>{children}</>
